# ─── Web UI Environment Variables ──────────────────
# Copy this file to .env and adjust values as needed.
#   cp .env.example .env

# Database — SQLite connection string (relative to prisma/ directory)
DATABASE_URL="file:./db/custom.db"

import { describe, it, expect } from 'vitest'

describe('api placeholder', () => {
  it('placeholder test — api.ts exports fetch-based functions requiring network mocking', () => {
    expect(true).toBe(true)
  })
})

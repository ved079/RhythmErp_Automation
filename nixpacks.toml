[phases.setup]
nixPkgs = ["python313", "chromium"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "CHROME_BIN=$(which chromium) CHROMEDRIVER_PATH=$(which chromedriver) uvicorn api.server:app --host 0.0.0.0 --port $PORT"
